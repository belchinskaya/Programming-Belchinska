#include "lib.h"

int main() {
    char * array1 = malloc(100 * sizeof(char));
    for (int i = 0; i < 100; i++) {

    }

    insert("abracadabra", "TEXT2", 4);
    delete_array("abracadabra", 4, 8);

    return 0;
}
