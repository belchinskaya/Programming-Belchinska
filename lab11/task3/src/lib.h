#pragma once
#include <stdlib.h>
#include <time.h>

#define N 10

void fillArray(float* arrayIn, int lenIn);
int getSequence (float* arrIn, int lenIn);
void getResultArray (float* arrIn, int lenIn, int* countMax, float* arrOut);

