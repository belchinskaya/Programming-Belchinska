#include "lib.h"
#include <stdio.h>

int main() {
    FILE *file = fopen("/Kate/firms.txt", "r");

 /*   long count = fileSize(file);
    pFirms = (char*) malloc (count * sizeof(char));
    readFromFile(pFirms, count);
    writeToConsole(pFirms, count);

    readFromFile(pFirms);

    struct Instrument violin;
    strcpy(violin-firm, "Stenor");

    fclose(file);
    */
    return 0;
}


