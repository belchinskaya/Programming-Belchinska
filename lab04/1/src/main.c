#define PI 3.14
#define R 3
//#define L 18.84
//#define S 28.26
//#define V 113.04
int main() {
int L;
int S;
int V;
int formula;
if (formula == L) {
	L = 2 * PI * R;
} else if (formula == S) {
	S = PI * R * R;
} else if (formula == V) {
	V = 4 / 3 * (PI * R * R * R);
}
return 0;
}
